money = int(input("Вводите сумму, которую планируете положить под проценты:"))
per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

max_key = max(per_cent, key=per_cent.get) # определяем ключ с максимальным значением

print("Максимальная сумма, которую вы можете заработать ", max(per_cent.values())*money/100, "рублей, в банке", max_key)
# max(per_cent.values()) - определяем максимальное значение в словаре
# в задаче намеренно не используется "список", так как задача простая и решаемая со словарем
